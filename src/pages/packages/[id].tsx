import PackageDetailContainer from "@containers/package-detail/PackageDetailContainer";
import React, { FC } from "react";

const PackageDetail: FC = () => {
  return <PackageDetailContainer />;
};

export default PackageDetail;
